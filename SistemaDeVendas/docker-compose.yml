services:
  sistema-de-vendas-api:
    image: jonathanbarr0s/sistema-de-vendas:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - CONNECTION_STRING=Host=db;Port=5432;Database=sistema_de_vendas;Username=postgres;Password=123456
    ports:
      - "8080:8080"
      - "8443:8443"
    depends_on:
      - db
    networks:
      - api-network

  db:
    image: postgres:15
    container_name: sistemavendas_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: sistema_de_vendas
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - api-network

networks:
  api-network:
    driver: bridge

volumes:
  db_data:
